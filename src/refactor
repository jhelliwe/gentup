Check for failure in syste command and return it to caller
Get outut from system command and return a String

Automatically call eix-sync (I think it already does)
IF it fails,exit

test for portage upgrade, dont ask just do it
test for gcc upgrade, ditto

Get an eix-diff for only packages that are to be ugrades and display it to user
